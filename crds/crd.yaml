apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: customconfigmaps.config.example.com
  labels:
    app.kubernetes.io/name: custom-configmap
    app.kubernetes.io/version: "1.0.0"
spec:
  group: config.example.com
  versions:
  - name: v1
    served: true
    storage: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              data:
                type: object
                additionalProperties:
                  type: string
                description: "Key-value pairs of configuration data"
              binaryData:
                type: object
                additionalProperties:
                  type: string
                  format: byte
                description: "Binary data encoded in base64"
              immutable:
                type: boolean
                default: false
                description: "If set to true, ensures that data stored in the CustomConfigMap cannot be updated"
            description: "CustomConfigMap specification"
          status:
            type: object
            properties:
              phase:
                type: string
                enum:
                - "Active"
                - "Failed"
                default: "Active"
                description: "The phase of the CustomConfigMap"
              lastUpdated:
                type: string
                format: date-time
                description: "Last time the CustomConfigMap was updated"
              dataHash:
                type: string
                description: "Hash of the data for change detection"
            description: "CustomConfigMap status"
        required:
        - spec
    additionalPrinterColumns:
    - name: Data Keys
      type: string
      description: "Number of data keys"
      jsonPath: ".spec.data"
    - name: Age
      type: date
      jsonPath: ".metadata.creationTimestamp"
    - name: Phase
      type: string
      jsonPath: ".status.phase"
    subresources:
      status: {}
  scope: Namespaced
  names:
    plural: customconfigmaps
    singular: customconfigmap
    kind: CustomConfigMap
    shortNames:
    - ccm
    categories:
    - all
