1. Your CustomConfigMap has been deployed successfully!

2. To check the status of your CustomConfigMap:
   kubectl get customconfigmap {{ .Values.customConfigMap.name | default (include "argo-tests.fullname" .) }} -n {{ .Release.Namespace }}

3. To describe your CustomConfigMap:
   kubectl describe customconfigmap {{ .Values.customConfigMap.name | default (include "argo-tests.fullname" .) }} -n {{ .Release.Namespace }}

4. To view the data in your CustomConfigMap:
   kubectl get customconfigmap {{ .Values.customConfigMap.name | default (include "argo-tests.fullname" .) }} -n {{ .Release.Namespace }} -o yaml

5. You can also use the short name 'ccm':
   kubectl get ccm -n {{ .Release.Namespace }}

{{- if .Values.customConfigMap.data }}
Your CustomConfigMap contains the following data keys:
{{- range $key, $value := .Values.customConfigMap.data }}
  - {{ $key }}
{{- end }}
{{- end }}
