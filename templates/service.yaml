apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-tests.fullname" . }}
  labels:
    {{- include "argo-tests.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-tests.selectorLabels" . | nindent 4 }}
